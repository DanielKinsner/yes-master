// Stable public entry point for the newest published full release.
// Beta releases are deliberately published as full GitHub releases (their tag
// and title still say beta) so this URL and the Tauri `/latest` updater channel
// always resolve to the same owner-approved build.
export const BETA_DOWNLOAD_URL =
  "https://github.com/DanielKinsner/yes-master/releases/latest";
