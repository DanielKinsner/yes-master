<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Pure-Compose app: the real theming lives in code (Material3
         darkColorScheme); this is just the pre-Compose window shell. -->
    <style name="Theme.YESMaster" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">#0B0D10</item>
        <item name="android:statusBarColor">#0B0D10</item>
    </style>
</resources>
