# Universal 50: complete original-source first-candidate coverage

Frozen before new audio on `codex/mastering-dynamics-research`, continuing from
`93b93b8a`. This is C1 development evidence, not a new mastering policy or C2
holdout. The owner has authorized continuing evidence collection and favors
preserving punch/section contrast in experiments. Existing presets stay intact.

## Question and fixed matrix

Does the previously tested source-relative single candidate at **Universal 50**
generalize beyond Funk/Rich, and does driving it toward -9 instead of -14 improve
delivery enough to justify its measured character changes?

Use the six remaining original development sources: **Coat, Piano, Imaginal,
Metal, Aphelion and Baby**. At each of **-14/-9 LUFS**, compare the corrected
current chain with the existing normalized zero-offset single candidate:
**24 new complete WAVs**, 48 kHz stereo float, -1 dBTP. Reuse the completed eight
Funk/Rich Universal 50 rows by exact evidence/file identity in the final joined
table; do not render them again. Loud 75 evidence remains in its prior record.

Bind the original C1 reports, source hashes, requested settings, operating drive,
coefficient strings and source-anchored section/transient measurements. Use Coat
v1 and the other five sources' v2 reports, as in the previous development probes.
Decode/peak-normalize once per source, shared across its four new outputs. This
does not rerun source analysis or change the single-candidate formula.

Use the existing corrected production chain/SRC/finalizer and unchanged C1
character limits, including the 0.2 LU research target tolerance. Tone/stereo
comparisons use the matched current control for the same source/preset/target.
Apply the frozen `control-inclusive-preserving-v1` selector to each pair only
after both files independently pass complete delivery verification. Retain
target shortfalls, unavailable measurements and character-fallback failures.
No extra offsets or search, new thresholds, composite score, changes to preset
coefficients or replacement of previous failed evidence within this checkpoint.

Report all individual indicators and both candidates, not only selected winners.
Two targets of the same track are related development observations, not independent
examples or a statistical sound-quality claim. Passing numerical checks does not
establish listening preference. A default intensity or a universal processing
reference cannot be chosen solely from this matrix.

## Verification, cost and storage

Independently check every new whole WAV with the frozen SOXR16/64 peak and LUFS
references plus finite/full-scale, exact frame/rate/channel and hash checks.
Require native qualified peaks at or below the requested ceiling. Join only
complete native/metric/reference/process reports with matching hashes. Recheck
retained Funk/Rich evidence and WAV hashes; reuse their original observations,
without relabeling old timing or measurements as fresh.

Freeze/copy the release probe and bind its hash before execution. Require 25 GiB
free reserve plus the bounded WAV estimate after building. Never replace or
delete earlier audio, evidence or protocols. No private audio is committed.

Measure once-per-source decode/normalization, chain, SRC, finalization, character
metrics, independent references and native child wall/CPU/peak memory separately.
Analysis/anchors are reused with their cost excluded from these new timings.
Run native rendering by itself to avoid deliberately competing research jobs;
then measure character/reference jobs separately. Record actual job scheduling.
These are offline preparation/evaluation costs, not app import, settings-change
settling, playback response or a complete user session. Reuse prior native
responsiveness and prepared-fact performance evidence for unchanged code.

## Decision boundary

This completes corrected-chain original-level Universal 50 control/single
coverage across the eight known sources. Broader search, very quiet copies,
other preset coverage, a source/preset/control contract, frozen cost limits and
new licensed holdout remain separate. If the simple rule fails, report where
and recommend a bounded follow-up based on the observed failure rather than
changing the limits to label it successful. Main and normal product behavior
remain unchanged by this research checkpoint.
