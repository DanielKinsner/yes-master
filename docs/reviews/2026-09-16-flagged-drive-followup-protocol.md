# Bounded follow-up for the two flagged single candidates

Frozen after the five-source single-first metric results, before new follow-up
audio. Run only after that checkpoint's independent whole-file checks complete.
Research branch: `codex/mastering-dynamics-research`; main remains `ab420654`.

The unchanged C1 metrics flag Metal's highest frequency band and Rich's section
contrast plus six tone bands. The single candidate satisfies the same target and
character limits on Funk, Aphelion and Baby; do not render more candidates for
those tracks in this follow-up.

For **Metal and Rich only**, compare additional input offsets **-3, -6 and -12 dB**
from the existing normalized single rule, compensating after the unchanged chain
before production SRC/finalization. This reuses the prior lower-grid hypothesis;
it is not a new preset retune. Fixed delivery remains Universal 75, Custom -14 LUFS,
-1 dBTP, 48 kHz stereo float. Bind the original settings/coefficient strings and
source hashes. No source profile, target, curve, character allowance or C1 tolerance
changes. A missing metric still fails its constraint.

Retain and reuse the freshly verified current control and zero-offset candidate
for each track: four existing whole outputs. Reuse requires matching source,
settings, output hash and complete independent evidence. Add six complete WAVs;
verify each independently. Expected aggregate: ten rows, four reused/six fresh.

Assess dynamics, source-anchored tone/stereo, target error and peak independently.
Report both character-preserving and target-first hypothetical choices. Do not
assume lower input monotonically improves quality, expand this grid to find a
pass, adopt a candidate or relax limits. If no candidate qualifies, retain that
result and a truthful current-control fallback; no automatic shipping policy is
selected. Current controls may themselves miss the target.

Measure raw source decode/normalization once per source, extra chain/SRC/finalizer
work, metrics and independent checks separately. Report total control-plus-single
cost from the first checkpoint and the actual additional bounded-search cost.
No estimated four-times multiplier and no product responsiveness claim.

Build/copy first, then check the 25 GiB reserve plus the six new WAVs' exact-size
estimate. Preserve all baseline and earlier evidence. This remains C1 development
on known sources; C2 holdout, C3 integration and owner sonic decisions remain open.
